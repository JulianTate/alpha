# Risk

Manual execution only. No broker credentials or order APIs exist. Signals include invalidation language but this first slice does not size positions or enforce live exits. Treat every signal as research/paper mode until enough out-of-sample evidence exists. Do not infer profitability from the demo or from raw return.
